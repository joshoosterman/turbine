TODO(joshoosterman): Proper readme

Requires:

SFML:
>  sudo apt-get install libsfml-dev
SDL:
> sudo apt-get install libsdl1.2-dev libsdl-image1.2-dev libsdl-mixer1.2-dev libsdl-ttf2.0-dev
(why both?)
